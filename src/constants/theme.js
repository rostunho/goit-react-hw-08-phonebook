export const themeLight = Object.freeze({
  colors: {
    appBg: '#E7E7E7',
  },

  spacing: value => `${4 * value}px`,
});

export const themeDark = Object.freeze({
  colors: {
    appBg: '#363636',
  },

  spacing: value => `${4 * value}px`,
});
